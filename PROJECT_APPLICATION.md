# MoonBook 项目申报书

## 基本信息

- 项目名称：MoonBook：mdBook 核心能力的 MoonBit 移植
- 参赛者：桌角的眼镜
- 联系方式：18875991125
- GitHub 仓库链接：[spectacleCase/moonbook.git](https://github.com/spectacleCase/moonbook.git)
- 项目方向：MoonBit 文档生成工具 / 静态站点生成器 / 开发者工具
- 是否为移植项目：是

## 项目简介

MoonBook 计划将 mdBook 的核心文档组织与静态网站生成能力移植到 MoonBit 生态，为 MoonBit 库、工具链、教程、课程和其他开源项目提供可复用的文档构建工具。用户可以使用 Markdown 编写内容，通过 `SUMMARY.md` 管理章节结构，并使用 MoonBook 生成包含目录导航、章节跳转、代码展示、主题和全文搜索的静态文档网站。

项目主要面向希望为 MoonBit 项目建立独立文档站的库作者和工具开发者。生成结果为普通 HTML、CSS、JavaScript 和静态资源，不依赖数据库或服务端运行环境，可直接部署到 GitHub Pages、静态文件服务器和对象存储。

MoonBook 不会重新实现完整 Markdown 解析器，而是优先复用 MoonBit 生态中已有的 Markdown、HTML、文件系统和异步网络能力，将主要开发工作放在章节模型、构建流程、主题渲染、资源管理、搜索索引和本地预览工具上。

## 项目现有基础

项目通过初审后已进入开发阶段，目前完成以下工作：

- 初始化 `spectacleCase/moonbook` MoonBit 模块和 Git 仓库；
- 配置项目名称、版本、仓库地址、关键词和 MPL-2.0 许可证；
- 实现可运行的 `init`、`build`、`check`、`serve` CLI；
- 实现配置、目录、渲染、搜索、链接检查和内存构建 API；
- 提供自动化测试和可实际构建的多层中文示例；
- 提供中文 README，说明项目定位、计划功能、非目标和开发命令；
- 配置 GitHub Actions，计划在多个操作系统上执行检查、测试和格式验证；
- 完成本项目申报书，明确移植来源、开发边界和技术路线。

当前已完成首个可用版本，后续将继续扩充边界测试、文档、资源处理和跨平台验证，并按验收要求发布到 mooncakes.io。

## 核心功能范围

- 提供 `moonbook init`，生成配置文件、`SUMMARY.md`、源码目录和示例章节；
- 提供 `moonbook build`，读取文档工程并输出可独立部署的静态网站；
- 提供 `moonbook serve`，启动本地 HTTP 预览服务；
- 解析 `SUMMARY.md`，支持章节、嵌套章节、分组标题和分隔项；
- 建立 Book、Chapter、Section、BuildContext 等 MoonBit 数据模型；
- 读取 Markdown 章节并转换为 HTML 页面；
- 生成侧边栏、上一页/下一页导航、面包屑、标题锚点和目录；
- 支持代码块、表格、任务列表、引用和常见扩展内容；
- 支持默认主题、自定义主题、附加 CSS/JavaScript 和静态资源复制；
- 生成客户端搜索索引，支持按标题、正文和章节路径检索；
- 检查章节文件、内部链接、锚点和静态资源引用；
- 支持增量构建的基础能力，仅重新处理发生变化的章节和资源；
- 提供配置、章节模型、构建器和渲染器的 MoonBit API；
- 提供可实际执行的示例工程、中文文档、测试和 CI；
- 按要求发布到 mooncakes.io，并保留可追踪的提交与版本记录。

## 计划开发内容

### 第一阶段：工程模型与基础构建

- 定义配置、章节和构建结果数据模型；
- 实现 `moonbook init` 与项目目录初始化；
- 实现 `SUMMARY.md` 的核心语法解析；
- 实现章节文件读取、顺序整理和错误诊断；
- 建立端到端示例和测试夹具。

### 第二阶段：HTML 输出与主题

- 接入 MoonBit 生态中的 Markdown 解析与渲染能力；
- 实现页面模板、章节导航、目录和标题锚点；
- 实现默认主题和静态资源处理；
- 支持用户附加 CSS、JavaScript 和自定义模板；
- 完善 HTML 转义、路径处理和跨平台输出。

### 第三阶段：搜索、预览与工程完善

- 生成可在浏览器中使用的全文搜索索引；
- 实现本地预览 HTTP 服务；
- 实现内部链接、锚点和资源检查；
- 增加基础增量构建和文件变化处理；
- 完善错误信息、README、用户指南和示例项目；
- 配置多平台 CI，完成 mooncakes.io 发布。

## 预期目标与技术路线

项目预计形成约 4,000 至 8,000 行有效 MoonBit 代码，采用分层包结构组织：配置层负责读取工程配置，Book 模型层负责章节和目录结构，构建层负责任务调度和资源处理，渲染层负责 HTML 与主题输出，服务层负责本地预览。

Markdown 语法处理优先复用 MoonBit 生态已有实现，避免与现有基础库重复。MoonBook 主要提供文档工程所需的上层能力，并通过抽象渲染接口降低对单一 Markdown 实现的耦合。文件系统和本地 HTTP 服务优先使用 MoonBit 官方或社区维护的异步基础设施。

测试将覆盖配置解析、目录解析、章节排序、路径规范化、链接处理、模板渲染、搜索索引和端到端构建。项目会准备小型、多层目录、中文路径、错误链接和自定义主题等不同示例，确保生成结果可以实际打开和部署。

## 移植或参考说明

- 原项目名称：mdBook
- 原项目链接：https://github.com/rust-lang/mdBook
- 原项目许可证：Mozilla Public License 2.0
- 本项目许可证：Mozilla Public License 2.0
- 本次移植范围：文档工程初始化、`SUMMARY.md` 章节组织、静态 HTML 构建、主题与资源处理、搜索索引、本地预览和基础链接检查

与原项目相比，本项目会做以下简化和重新设计：

- 使用 MoonBit 原生包结构、类型系统、错误处理和测试方式，而不是复制 Rust workspace 和 crate 结构；
- 优先完成适合 MoonBit 项目使用的核心文档构建流程，不追求与 mdBook 所有命令行参数完全兼容；
- Markdown 解析优先复用 MoonBit 生态现有库，不把 CommonMark 解析器本身列入本次重复实现范围；
- 首版只输出静态 HTML，不支持 PDF、EPUB 等排版格式；
- 首版不完整移植 mdBook 的第三方预处理器和后端插件协议，只保留后续扩展所需的接口边界；
- 主题系统采用更轻量的模板和静态资源结构，默认提供一套可用主题；
- 搜索功能生成浏览器端可加载的静态索引，不引入外部搜索服务；
- 本地预览服务只用于开发阶段，不定位为生产环境 Web 服务器；
- 对上游代码、设计和测试用例的使用保留必要版权、许可证与来源说明。

## 预计完成的测试、文档与交付物

- 公开且可以正常访问的 GitHub 仓库；
- 可构建、可运行的 MoonBit CLI；
- 配置、章节、构建、渲染和搜索等核心包；
- 覆盖正常输入、边界输入和错误输入的自动化测试；
- 至少一个可以完整构建和浏览的中文示例文档站；
- 中文 README、安装说明、命令参考、配置参考和主题说明；
- GitHub Actions 多平台 CI；
- 版本更新记录、迁移说明和许可证来源说明；
- 发布至 mooncakes.io 的可安装版本。

## 项目价值与维护计划

MoonBit 生态已经具备 Markdown、HTML、文件系统和 HTTP 等基础能力，但从一组 Markdown 文件到可部署文档网站仍需要章节组织、构建流程、导航、主题、搜索和预览等上层工具。MoonBook 将这些能力组合成可直接使用的软件，既可以服务自身文档，也可以成为后续 MoonBit 教程、课程、API 指南和开源项目文档的共同基础设施。

验收后将继续跟进 MoonBit 工具链和依赖库更新，维护主要构建流程、示例和兼容性测试。后续根据实际使用反馈，再评估插件协议、多主题和其他输出格式，不在首期过度扩张范围。
